DEF_FULL_NAME="S-Lang Library"
DEF_NAME="slang"
DEF_SHA="75b2c90421d34d54246dfaaffe829b2d33cf733c"
DEF_VERSION="2.2.4"
DEF_HTTP_PATH="${MAIN_SOURCE_REPOSITORY}${DEF_NAME}-${DEF_VERSION}.tar.gz"
DEF_REQUIREMENTS="libiconv pcre libpng"
DEF_CONFIGURE_ARGS="--without-onig"
DEF_AFTER_INSTALL_METHOD="${MKDIR_BIN} ${PREFIX}/include/slang; ${LN_BIN} -s ${PREFIX}/include/slang.h ${PREFIX}/include/slang/slang.h"
DEF_MAKE_METHOD="make -s"
DEF_INSTALL_METHOD="make -s install"
