. ${DEFINITIONS_DIR}ffmpeg.def

APP_POSTFIX="lib"
APP_CONFIGURE_ARGS="${APP_CONFIGURE_ARGS} ${FFMPEG_DEFAULT_CONFIGURE_FLAGS} \
    --disable-programs --disable-devices --enable-shared --enable-static"
