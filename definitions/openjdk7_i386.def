DEF_DISABLE_ON="Darwin Linux"
DEF_FULL_NAME="OpenJDK 1.7 32bit"
DEF_NAME="Openjdk7-i386"
DEF_VERSION="1.7.0_06"
DEF_SHA="48fefe5362679bb581e034c1dccc6902039e12bf"
DEF_SOURCE_PATH="${MAIN_BINARY_REPOSITORY}${DEF_NAME}_${DEF_VERSION}.tar.gz"
DEF_CONFIGURE="binary"
DEF_AFTER_INSTALL_METHOD="${MV_BIN} ${CACHE_DIR}cache/${DEF_NAME}-${DEF_VERSION}/${DEF_NAME} ${PREFIX}; ${RM_BIN} -f ${PREFIX}/lib/libiconv.so.3 ; ${RM_BIN} -f ${PREFIX}/lib/libcharset.so.1 ; ${RM_BIN} -f ${PREFIX}/lib/libintl.so.9"
DEF_USER_INFO="OpenJDK notes:
  1. This is FreeBSD binary build.
  2. This definition also sets JAVA_HOME to /Software/YOUR-VERSION.
  3. Consider using own JAVA_HOME to override default value for user side Openjdk.
  4. Whole JAVA ENV is set through $HOME/.profile_java"
