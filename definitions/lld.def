inherit llvm

DEF_FULL_NAME="${DEF_FULL_NAME} + LLVM Linker"
DEF_REQUIREMENTS="${DEF_REQUIREMENTS} libffi"
DEF_EXPORTS="${DEF_EXPORTS} lld lldb"
DEF_CONFIGURE_ARGS="${DEF_CONFIGURE_ARGS} -DLLVM_ENABLE_LIBCXX=on -DLLVM_TOOL_LLDB_BUILD=on -DLLVM_TOOL_LLD_BUILD=on -DLLVM_BUILD_TOOLS=on -DLLVM_INCLUDE_TOOLS=on -DLLVM_INCLUDE_UTILS=on -DLLVM_ENABLE_CXX1Y=on -DLLVM_ENABLE_FFI=on -DFFI_LIBRARY_DIR=${PREFIX}/lib -DFFI_INCLUDE_DIR=${PREFIX}/include"
