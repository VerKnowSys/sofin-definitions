. ${DEFINITIONS_DIR}php4.def

APP_CONFIGURE_ARGS="$(echo "${APP_CONFIGURE_ARGS}" | ${SED_BIN} -e 's/--with-litespeed/--enable-cli/') --disable-cli --enable-spl --with-expat-dir=${PREFIX} --enable-fastcgi --enable-cgi"

install_pear_modules () {
    echo "NOOP"
}

install_extensions () {
    echo "NOOP"
}
