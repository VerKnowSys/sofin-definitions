DISABLE_ON="Darwin"
APP_STANDALONE="true"
APP_FULL_NAME="Zabbix monitoring system"
APP_NAME="zabbix"
APP_SHA="2de4f6f245e02786ff51747db5fad933b626387a"
APP_VERSION="3.0.2"
APP_HTTP_PATH="${MAIN_SOURCE_REPOSITORY}${APP_NAME}-${APP_VERSION}.tar.gz"
COMMON_ZABBIX_CONF_ARGS="--enable-ipv6 --enable-agent --with-libcurl --with-sqlite3 --with-iconv=${PREFIX} --with-iconv-include=${PREFIX}/include --with-iconv-lib=${PREFIX}/lib --with-openssl=${PREFIX}"
APP_CONFIGURE_ARGS="--enable-proxy --enable-server ${COMMON_ZABBIX_CONF_ARGS}"
if [ "${SYSTEM_NAME}" = "Linux" ]; then
    APP_CONFIGURE_ARGS="${APP_CONFIGURE_ARGS} --sysconfdir=/etc"
fi
APP_REQUIREMENTS="pkgconf gmake libiconv m4 perl texinfo libidn curl readline-dynamic sqlite openssl"
APP_EXPORTS="zabbix_get zabbix_sender zabbix_agent zabbix_agentd zabbix_proxy zabbix_server"
APP_USELESS="include share/texinfo lib/*perl*"
