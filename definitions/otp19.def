DEF_FULL_NAME="Erlang OTP"
DEF_SHA="6f394857b2910de27b39275cdad9ed05be95bcda"
DEF_NAME="otp"
PATCHES_SITE="https://olgeni.olgeni.com/~olgeni/distfiles/"
DEF_STANDALONE=YES
MAJOR="19.3"
MINOR="6.5"
DEF_VERSION="${MAJOR}.${MINOR}"
DEF_SOURCE_PATH="${MAIN_SOURCE_REPOSITORY}${DEF_NAME}_src_${MAJOR}.tar.gz"
DEF_REQUIREMENTS="pkgconf make m4 expat zlib lzma lzo xz gettext db gdbm libunwind libressl perl bison flex libgpg-error libgcrypt autoconf texinfo automake18"

DEF_AFTER_UNPACK_METHOD="post_unpack"
post_unpack () {
    note "Removing prebuilt files"
    ./otp_build remove_prebuilt_files

    note "Otp autoconf"
    ./otp_build autoconf
}

DEF_CONFIGURE_ARGS="--enable-threads --with-dynamic-trace=dtrace --enable-smp-support --with-ssl=${PREFIX}  --enable-native-libs --without-odbc --without-docs --enable-sctp --enable-kernel-poll"
DEF_INSTALL_METHOD="make -s install && install_repl_history"
DEF_EXPORTS="ct_run dialyzer epmd erl erlc escript run_erl to_erl typer"
DEF_CONFLICTS_WITH="Otp Elixir"
DEF_USELESS="lib/*perl* docs share"
DEF_NO_TRAP_INT_OVERFLOW=YES
DEF_NO_LTO=YES
DEF_NO_CCACHE=YES
DEF_USE_SAFE_STACK=YES
case "${SYSTEM_NAME}" in
    FreeBSD)
        DEF_COMPILER_FLAGS="${DEF_OPTFLAGS} -fPIC -fPIE -DMAP_NORESERVE=0 -fno-omit-frame-pointer -fsanitize=safe-stack"
        DEF_LINKER_FLAGS="-L${PREFIX}/lib -L/usr/lib -ldtrace -lthr -lpthread -pie -fsanitize=safe-stack"

        # NOTE: this causes a hickup for HIPE:
        # erl_prettypr.erl: internal error in native_compile;
        # crash reason: undef
        # (...)
        #
        # DEF_CONFIGURE_ARGS="${DEF_CONFIGURE_ARGS} --enable-m64-build"
        DEF_CONFIGURE_ARGS="${DEF_CONFIGURE_ARGS} --disable-wx --disable-java --enable-hipe"
        ;;

    Darwin)
        DEF_REQUIREMENTS="${DEF_REQUIREMENTS} jpeg jbig2dec jbigkit nasm libjpeg-turbo tiff libpng wxwidgets"
        DEF_CONFIGURE_ARGS="${DEF_CONFIGURE_ARGS} --enable-wx --enable-java --enable-darwin-64bit --enable-hipe"
        ;;
esac

HISTORY_PLUGIN_SOURCE="https://github.com/VerKnowSys/erlang-history"
install_repl_history () {
    run "${GIT_BIN} clone ${HISTORY_PLUGIN_SOURCE}"
    cd erlang-history
    note "Installing erlang-history plugin"
    run "PATH=$PATH:${PREFIX}/bin:/bin make install"
}
