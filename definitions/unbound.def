DISABLE_ON="Darwin"
APP_FULL_NAME="Unbound DNS"
APP_STANDALONE="true"
APP_SHA="1391888d2e3395d766545cd3dbdf0f1879c48080"
APP_NAME="unbound"
APP_VERSION="1.5.8"
APP_HTTP_PATH="${MAIN_SOURCE_REPOSITORY}${APP_NAME}-${APP_VERSION}.tar.gz"
APP_REQUIREMENTS="expat libevent libressl"
APP_CONFIGURE_ARGS="--with-libexpat=${PREFIX} --with-ssl=${PREFIX} --with-libevent=${PREFIX}"
APP_EXPORTS="unbound unbound-anchor unbound-checkconf unbound-control unbound-control-setup"
