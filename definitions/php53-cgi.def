inherit php53

DEF_CONFIGURE_ARGS="${DEF_CONFIGURE_ARGS} --with-openssl=${PREFIX} --disable-fpm --enable-cli --without-apxs2 --without-litespeed --enable-cgi --enable-fastcgi"

install_pear_modules () {
    echo "NOOP"
}

install_extensions () {
    echo "NOOP"
}
