inherit zabbix

APP_FULL_NAME="Zabbix agent"
APP_POSTFIX="-agent"
APP_CONFIGURE_ARGS="--disable-proxy --disable-server ${COMMON_ZABBIX_CONF_ARGS}"
if [ "${SYSTEM_NAME}" = "Linux" ]; then
    APP_CONFIGURE_ARGS="${APP_CONFIGURE_ARGS} --sysconfdir=/etc"
fi
