inherit boost

DEF_FULL_NAME="Boost - C++ Libraries - minimal configuration"
DEF_CONFIGURE_ARGS="${DEF_CONFIGURE_ARGS} --without-icu --with-libraries=system,thread"
DEF_MAKE_METHOD="./b2 -j${CPUS} variant=release link=static runtime-link=static threading=multi install"
