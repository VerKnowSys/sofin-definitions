inherit boost

DEF_FULL_NAME="${DEF_FULL_NAME} - minimal configuration"
DEF_REQUIREMENTS="make python27"
DEF_CONFIGURE_ARGS="--with-toolset=clang --with-python-root=${PREFIX} --with-python=${PREFIX}/bin/python --libdir=${PREFIX}/lib --includedir=${PREFIX}/include --without-icu --with-libraries=system,thread"
