. ${DEFINITIONS_DIR}boost.def

APP_FULL_NAME="Boost - C++ Libraries - minimal configuration"
APP_CONFIGURE_ARGS="${APP_CONFIGURE_ARGS} --without-icu --with-libraries=system,thread"
APP_MAKE_METHOD="./b2 -j8 variant=release link=static runtime-link=static threading=multi install"
