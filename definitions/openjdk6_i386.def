DEF_DISABLE_ON="Darwin Linux"
DEF_FULL_NAME="OpenJDK 1.6 32bit"
DEF_SHA="9bcf53e1cf2d55876b7edf1bbf51e972d7f4a770"
DEF_NAME="Openjdk6-i386"
DEF_VERSION="1.6.0_32"
DEF_HTTP_PATH="${MAIN_BINARY_REPOSITORY}${APP_NAME}_${APP_VERSION}.tar.gz"
DEF_CONFIGURE="binary"
DEF_AFTER_INSTALL_CALLBACK="${CP_BIN} -fR ${CACHE_DIR}cache/${APP_NAME}-${APP_VERSION}/${APP_NAME}/ ${PREFIX}; ${RM_BIN} -f ${PREFIX}/lib/libiconv.so.3 ; ${RM_BIN} -f ${PREFIX}/lib/libcharset.so.1 ; ${RM_BIN} -f ${PREFIX}/lib/libintl.so.9"
DEF_USER_INFO="OpenJDK notes:
  1. This is FreeBSD binary build.
  2. This definition also sets JAVA_HOME to /Software/YOUR-VERSION.
  3. Consider using own JAVA_HOME to override default value for user side Openjdk.
  4. Whole JAVA ENV is set through $HOME/.profile_java"
